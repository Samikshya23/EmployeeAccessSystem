@model EmployeeAccessSystem.Models.FortigateItem
@{
    Layout = null;
}
<form asp-action="Create" method="post" id="createFortigateItemForm">
    @Html.AntiForgeryToken()

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }

    <div class="form-group">
        <label asp-for="FortigateCategoryId" class="font-weight-bold">Fortigate Category</label>
        <select asp-for="FortigateCategoryId" asp-items="ViewBag.FortigateCategoryList" class="form-control">
            <option value="">Select Fortigate Category</option>
        </select>
        <span asp-validation-for="FortigateCategoryId" class="text-danger small"></span>
    </div>

    <div class="form-group">
        <label asp-for="ItemName" class="font-weight-bold">Item Name</label>
        <input asp-for="ItemName" class="form-control" placeholder="Enter item name" />
        <span asp-validation-for="ItemName" class="text-danger small"></span>
    </div>

    <div class="form-group mb-4">
        <div class="custom-control custom-switch">
            <input asp-for="IsActive" class="custom-control-input" id="CreateIsActive" />
            <label class="custom-control-label font-weight-bold" for="CreateIsActive" id="createStatusLabel">Active</label>
        </div>
        <span asp-validation-for="IsActive" class="text-danger small"></span>
    </div>

    <div class="modal-footer px-0 pb-0">
        <button type="button" class="btn btn-default" data-dismiss="modal">Back</button>
        <button type="submit" class="btn btn-primary">Save</button>
    </div>
</form>

<partial name="_ValidationScriptsPartial" />

<script>
    $(document).ready(function () {

        function updateCreateStatus() {
            if ($('#CreateIsActive').is(':checked')) {
                $('#createStatusLabel').text('Active');
            } else {
                $('#createStatusLabel').text('Inactive');
            }
        }

        updateCreateStatus();

        $('#CreateIsActive').change(function () {
            updateCreateStatus();
        });

    });
</script>