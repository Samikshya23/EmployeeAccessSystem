@model EmployeeAccessSystem.Models.PingProductItem

@{
    Layout = null;
}

<form asp-action="Edit" method="post" id="editPingProductItemForm">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="PingProductItemId" />

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }

    <div class="form-group">
        <label asp-for="PingProductId" class="font-weight-bold">Ping Product</label>
        <select asp-for="PingProductId" asp-items="ViewBag.PingProductList" class="form-control">
            <option value="">Select Ping Product</option>
        </select>
        <span asp-validation-for="PingProductId" class="text-danger small"></span>
    </div>

    <div class="form-group">
        <label asp-for="ItemName" class="font-weight-bold">Item Name</label>
        <input asp-for="ItemName" class="form-control" placeholder="Enter Item Name" />
        <span asp-validation-for="ItemName" class="text-danger small"></span>
    </div>

    <div class="form-group mb-4">
        <div class="custom-control custom-switch">
            <input asp-for="IsActive" class="custom-control-input" id="EditIsActive" />
            <label class="custom-control-label font-weight-bold" for="EditIsActive" id="editStatusLabel">
                Active
            </label>
        </div>
    </div>

    <div class="modal-footer px-0 pb-0">
        <button type="button" class="btn btn-default" data-dismiss="modal">Back</button>
        <button type="submit" class="btn btn-warning">Update</button>
    </div>
</form>

<partial name="_ValidationScriptsPartial" />

<script>
    $(document).ready(function () {

        function updateEditStatus() {
            if ($('#EditIsActive').is(':checked')) {
                $('#editStatusLabel').text('Active');
            } else {
                $('#editStatusLabel').text('Inactive');
            }
        }

        updateEditStatus();

        $('#EditIsActive').change(function () {
            updateEditStatus();
        });

    });
</script>