@model EmployeeAccessSystem.Models.PingProduct

@{
    Layout = null;
}

<form asp-action="Edit" method="post" id="editPingProductForm">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="PingProductId" />

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }

    <div class="form-group">
        <label asp-for="ProductId" class="font-weight-bold">Product</label>
        <select asp-for="ProductId" asp-items="ViewBag.ProductList" class="form-control">
            <option value="">Select Product</option>
        </select>
        <span asp-validation-for="ProductId" class="text-danger small"></span>
    </div>

    <div class="form-group">
        <label asp-for="PingProductName" class="font-weight-bold">Ping Product Name</label>
        <input asp-for="PingProductName" class="form-control" placeholder="Enter Ping Product Name" />
        <span asp-validation-for="PingProductName" class="text-danger small"></span>
    </div>

    <div class="form-group mb-4">
        <div class="custom-control custom-switch">
            <input asp-for="IsActive" class="custom-control-input" id="EditIsActive" />
            <label class="custom-control-label font-weight-bold" for="EditIsActive" id="editStatusLabel">
                Active
            </label>
        </div>
        <span asp-validation-for="IsActive" class="text-danger small"></span>
    </div>

    <div class="modal-footer px-0 pb-0">
        <button type="button" class="btn btn-default" data-dismiss="modal">Back</button>
        <button type="submit" class="btn btn-warning">Update</button>
    </div>
</form>

<partial name="_ValidationScriptsPartial" />

<script>
    $(document).ready(function () {

        function updateEditStatus() {
            if ($('#EditIsActive').is(':checked')) {
                $('#editStatusLabel').text('Active');
            } else {
                $('#editStatusLabel').text('Inactive');
            }
        }

        updateEditStatus();

        $('#EditIsActive').change(function () {
            updateEditStatus();
        });

    });
</script>